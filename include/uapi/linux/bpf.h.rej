diff a/include/uapi/linux/bpf.h b/include/uapi/linux/bpf.h	(rejected hunks)
@@ -206,6 +206,7 @@ enum bpf_prog_type {
 	BPF_PROG_TYPE_EXT,
 	BPF_PROG_TYPE_LSM,
 	BPF_PROG_TYPE_SK_LOOKUP,
+	BPF_PROG_TYPE_XRP,
 };
 
 enum bpf_attach_type {
@@ -247,6 +248,7 @@ enum bpf_attach_type {
 	BPF_XDP_CPUMAP,
 	BPF_SK_LOOKUP,
 	BPF_XDP,
+	BPF_XRP,
 	__MAX_BPF_ATTACH_TYPE
 };
 
@@ -5253,4 +5255,12 @@ enum {
 	BTF_F_ZERO	=	(1ULL << 3),
 };
 
+struct bpf_xrp {
+	__s32 done;
+	__u64 next_addr[16];
+	__u64 size[16];
+	char *data;
+	char *scratch;
+};
+
 #endif /* _UAPI__LINUX_BPF_H__ */
